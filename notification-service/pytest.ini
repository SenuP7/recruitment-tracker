[pytest]
; tests/ lives outside lambda/ (the Lambda deployment package / CodeUri) on purpose --
; SAM's Python builder has no ignore-file mechanism, so anything inside lambda/ ships
; to AWS. This adds lambda/ to sys.path so tests can still `from notifier import ...`.
pythonpath = lambda
